
gcloud run services add-iam-policy-binding mermaidjs-internal \
  --region asia-south1 \
  --member=serviceAccount:902392910355-compute@developer.gserviceaccount.com \
  --role=roles/run.invoker


build and deploy:
gcloud run deploy mermaidjs-internal \
  --source . \
  --region=asia-south1 \
  --network=default \
  --subnet=default \
  --vpc-egress=private-ranges-only \
  --ingress=internal \
  --no-allow-unauthenticated \
  --cpu=1 \
  --memory=2Gi \
  --concurrency=2 \
  --min-instances=0 \
  --max-instances=1 \
  --timeout=300


deploy existing image:
gcloud run deploy mermaidjs-internal \
  --image="asia-south1-docker.pkg.dev/sap-ai-abap-specgen-prod-prj/cloud-run-source-deploy/mermaidjs-internal:latest" \
  --region=asia-south1 \
  --network=default \
  --subnet=default \
  --vpc-egress=private-ranges-only \
  --ingress=internal \
  --no-allow-unauthenticated \
  --cpu=1 \
  --memory=2Gi \
  --concurrency=2 \
  --min-instances=0 \
  --max-instances=1 \
  --timeout=300





gcloud run services proxy mermaidjs-internal --region=asia-south1 --port=8080

curl -X POST "127.0.0.1:8080/generate" \
     -H "Content-Type: application/json" \
     -d '{"code": "flowchart TD subgraph Program_Initialization Start["Report SD_SALES_DOCUMENT_VIEW"] InitEvent["INITIALIZATION Event"] CreateController["Create Controller Object
go_sdoc_view (CL_SDOC_VIEW)"] end Start --> InitEvent InitEvent --> CreateController subgraph Selection_Screen_Events ASO["AT SELECTION-SCREEN OUTPUT"] DefaultParams["Default Parameters via GET PARAMETER ID"] DisplayScreen["Display Selection Screen"] subgraph User_Interaction F4Help["ON VALUE-REQUEST
(go_sdoc_view->on_value_request_auart)"] F1Help["ON HELP-REQUEST
(go_sdoc_view->on_help_request_dataelement)"] end AS_Validate["AT SELECTION-SCREEN Validation
(go_sdoc_view->at_selection_screen)"] end CreateController --> ASO ASO --> DefaultParams DefaultParams --> DisplayScreen DisplayScreen --> F4Help DisplayScreen --> F1Help F4Help --> AS_Validate F1Help --> AS_Validate subgraph Main_Processing SOS["START-OF-SELECTION Event"] MapMain["Map Main Selections
(svbeln, sauart, skunnr...)"] MapSupp["Map Supplementary Selections
(spartner, svkorg, pvboff...)"] BuildSelCrit["Build Selection Criteria Structure (gs_selcrit)"] ExecuteLogic["Execute Main Logic
(go_sdoc_view->start_of_selection)"] end AS_Validate --> SOS SOS --> MapMain SOS --> MapSupp MapMain --> BuildSelCrit MapSupp --> BuildSelCrit BuildSelCrit --> ExecuteLogic subgraph Final_Output EOS["END-OF-SELECTION Event"] RenderOutput["Render Final Output
(go_sdoc_view->end_of_selection)"] end ExecuteLogic --> EOS EOS --> RenderOutput", "width": 800, "height": 600, "scale": 1.0}' \
     --output test.png
